import styles from "./SliderPane.module.scss";

const SliderPane = (images: string) => {
  return (
    <div className={styles.sliderPane}>
      <p>SliderPane</p>
    </div>
  );
};

export default SliderPane;
